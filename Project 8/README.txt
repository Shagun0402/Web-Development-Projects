Querying XML data using Xquery

Instructions to run the queries:

1) Put all queries and xml file in a folder.
2) Using Zorba Command Line cd to the folder in which files are present.
3) To run query and to parse it in a XML file:
	zorba -f -q query.xq >result.xml
